const SectionTitle = ({ title }: { title: string }) => {
  return (
    <h2 className=" capitalize text-center text-4xl py-10 mt-20 font-bold">
      {title}
    </h2>
  );
};

export default SectionTitle;
